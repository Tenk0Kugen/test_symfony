<!DOCTYPE html>
<html lang="ru">
	<head>
		<title>Главная</title>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<style>
			h1 {text-align: center; font-family: GothicI; font-size: 30px;}
			.categories {width: 15%; float: right; position: relative;}
			.products {width: 85%; float: left; position: relative;}
		</style>
	</head>
	<body>
		<div class="products">
			Товары:
			<br />
			{% for product in products %}
				<div class="product">
					<a href="{{ path('product', {product_name: product.name}) }}">{{ product.name }}</a>
				</div>
			{% endfor %}
		</div>
		<div class="categories">
			Категории:
			<br />
			{% for category in categories %}
				<div class="category">
					<a href="{{ path('category', {category_name: category.name}) }}">{{ category.name }}</a>
				</div>
			{% endfor %}
		</div>
	</body>
</html>
