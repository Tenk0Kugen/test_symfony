<!DOCTYPE html>
<html lang="ru">
	<head>
		<title>Продукт {{ product.name | default('not found') }}</title>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<style>
			h1 {text-align: center; font-family: GothicI; font-size: 30px;}
		</style>
	</head>
	<body>
		<h1>Продукт {{ product.name | default('not found') }}</h1>
		<div>ID: {{ product.id | default('-1') }}</div>
	</body>
</html>
